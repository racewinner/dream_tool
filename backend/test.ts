// Simple test file to verify TypeScript compilation
console.log('Hello, TypeScript!');

// A simple function with TypeScript type annotations
function greet(name: string): string {
  return `Hello, ${name}!`;
}

// Usage
const message = greet('World');
console.log(message);
